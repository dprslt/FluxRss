

{% macro News(id, titre, description,url, URLImage) %}
    <div class="center_news">
        {#{% if recent == false %}
            <div class="time_indicator_on"></div>
        {% else %}
            <div class="time_indicator_off"></div>
        {% endif %}#}
        <img src="{{ urlImage }}"/>
        {# description|raw permet de recuperer le code html brut #}
        <p><a href="{{ url }}">{{ titre }}</a> ~ {{ description|raw }}<p>
        {#<span> date </span>#}
    </div>
{% endmacro %}

{% macro Flux(id, titre, URL, resume, image) %}
    <div class="center_news">
        {#{% if recent == false %}
            <div class="time_indicator_on"></div>
        {% else %}
            <div class="time_indicator_off"></div>
        {% endif %}#}
        <a href="{{URL}}">{{titre}}</a>
        <img src="{{image}}"/>
        <p>{{resume}}</p>
        {#<span> date </span>#}
    </div>
        
{% endmacro %}