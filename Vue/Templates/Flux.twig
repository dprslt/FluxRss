{% import 'MacroFlux.twig' as VueFlux %}



<div class="center_newslist">

    {% if Admin %}
        <div class="flux_admin_add">
            skjghsfsj
        </div>
    {% endif %}

    {% for element in Fluxs %}
        {#Appelle macro pour afficher tous les Flux#}
        {{ VueFlux.Flux(element.id, element.title, element.link, element.description, element.imageUrl, element.imageLink, element.imageTitre,Admin) }}
    {% endfor %}
</div>


<div class="page_navigation">
    {%  if numpage > 1  %}
        <a href="?action=afficherFlux&page={{ numpage-1 }}">Pr�c�dent</a> |
    {% endif %}
    <a href="?action=afficherFlux&page={{ numpage+1 }}">Suivant</a>
</div>